name: CI

on:
  push:
    branches: ["**"]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: ci (dagger)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          path: shard-listener

      - uses: actions/checkout@v6
        with:
          repository: lightwebinc/shard-common
          path: shard-common

      - uses: actions/setup-go@v6
        with:
          go-version-file: shard-listener/ci/go.mod
          cache: false

      - name: Run Dagger pipeline
        working-directory: shard-listener
        env:
          GOWORK: "off"
        run: make ci

  e2e:
    name: e2e (host)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          path: shard-listener

      - uses: actions/checkout@v6
        with:
          repository: lightwebinc/shard-common
          path: shard-common

      - uses: actions/checkout@v6
        with:
          repository: lightwebinc/shard-proxy
          path: shard-proxy

      - uses: actions/setup-go@v6
        with:
          go-version-file: shard-listener/go.mod

      - name: Use local shard-common (listener)
        working-directory: shard-listener
        run: go mod edit -replace github.com/lightwebinc/shard-common=../shard-common

      - name: Use local shard-common (proxy)
        working-directory: shard-proxy
        run: go mod edit -replace github.com/lightwebinc/shard-common=../shard-common

      - name: Install curl
        run: sudo apt-get install -y --no-install-recommends curl

      - name: Enable IPv6 multicast on loopback
        run: |
          sudo ip link set lo multicast on
          sudo ip -6 route add ff00::/8 dev lo table local 2>/dev/null || true

      - name: E2E test
        working-directory: shard-listener
        run: make test-e2e
